import { Authentication.Service } from './authentication.service';

describe('Authentication.Service', () => {
  it('should create an instance', () => {
    expect(new Authentication.Service()).toBeTruthy();
  });
});
